{
  "name": "tinjau",
  "private": true,
  "packageManager": "pnpm@10.18.3",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r build",
    "snapshot:agents": "tsx scripts/snapshot-agents.mjs",
    "test": "pnpm -r test && pnpm test:contracts",
    "test:contracts": "cd contracts && forge test -vv",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^24.13.4",
    "ethers": "^6.17.0",
    "playwright-core": "^1.63.0",
    "tsx": "^4.23.13",
    "typescript": "~5.9.3",
    "vitest": "^5.0.0"
  }
}
